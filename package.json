{
  "name": "auth0-netlify",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npx eleventy",
    "dev": "npx eleventy --watch --serve",
    "develop": "npx eleventy --watch --serve",
    "now-build": "npm run build",
    "debug": "DEBUG=* npx eleventy"
  },
  "dependencies": {
    "@11ty/eleventy": ">=0.11.0",
    "@serverless-jwt/netlify": ">=0.2.0"
  }
}
